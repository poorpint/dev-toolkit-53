type ClickCoordinates = { x: number; y: number; }; class AutoClicker { private intervalId: NodeJS.Timeout | null = null; private clicksPerSecond: number; private clickPosition: ClickCoordinates; constructor(clicksPerSecond: number, clickPosition: ClickCoordinates) { this.clicksPerSecond = clicksPerSecond; this.clickPosition = clickPosition; } start(): void { if (this.intervalId) return; this.intervalId = setInterval(() => this.performClick(), 1000 / this.clicksPerSecond); } stop(): void { if (this.intervalId) { clearInterval(this.intervalId); this.intervalId = null; } } private performClick(): void { const element = document.elementFromPoint(this.clickPosition.x, this.clickPosition.y); if (element) { const event = new MouseEvent('click', { bubbles: true, cancelable: true, view: window }); element.dispatchEvent(event); } } } export default AutoClicker;